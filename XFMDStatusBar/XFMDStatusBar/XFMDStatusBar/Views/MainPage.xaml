<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="XFMDStatusBar.Views.MainPage"
             Title="MainPage">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label Text="{Binding Title}" />

        <Button Text="正常模式的導航抽屜" Command="{Binding 正常模式的導航抽屜Command}" />
        <Button Text="使用Padding的導航抽屜" Command="{Binding 使用Padding的導航抽屜Command}" />
        <Button Text="使用Renderer的導航抽屜" Command="{Binding 使用Renderer的導航抽屜Command}" />
    </StackLayout>
</ContentPage>